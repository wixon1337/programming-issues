const masodfok = (string) => {
  let x1 = 0;
  let x2 = 0;
  for (let i = 0; i < string.length; i++) {




  }
  return [x1, x2];
};

console.log(masodfok('2x^2-3x+6=9'));

/* if (string.charAt(i) === '3') { // || '2' || '3' || '4' || '5' || '6' || '7' || '8' || '9') {
      // process.stdout.write('szam'); */

let a = '-15';
console.log(Number(a) + 2);
let b;
